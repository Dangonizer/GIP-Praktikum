#pragma once
#include <string>
// using namespace std;
int zeichenkette_suchen_rekursiv(std::string text,
                                 std::string zkette,
                                 size_t text_pos = 0,
                                 size_t text_such_pos = 0,
                                 size_t zkette_such_pos = 0);
